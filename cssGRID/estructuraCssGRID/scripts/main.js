/*  window.addEventListener('scroll',function(){
    let animacion=document.getElementById('cont-form');
    let pos=animacion.getBoundingClientRect().top;
    console.log(pos);
    let tamañoP=window.innerHeight; //codigo para hacer animacion con css pero cuando vaya bajando 
                                      //el scroll se ejecutara la animacion

    if(pos < tamañoP){
        animacion.style.animation='atencionmensaje  1s ease-out'
    }
})
  */

  